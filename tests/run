#/bin/bash

DIABLO="$(cd `dirname $0`/..; pwd -P)"
OPENMPI_HOME=/home/$USER/.openmpi

export PATH="$PATH:$OPENMPI_HOME/bin"
export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:$OPENMPI_HOME/lib/"

mpirun -N 2 scala -classpath classes:${DIABLO}/lib/diablo.jar:${OPENMPI_HOME}/lib/mpi.jar -J-Xmx1g -J-Xss100m $*
